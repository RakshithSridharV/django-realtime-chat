version: '3'

services:
  web:
    build: .
    command: daphne djangochat.asgi:application -b 0.0.0.0 -p 8000
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: redis
    ports:
      - "6379:6379"